clear
set verbose off

include /home/at/git/PairPlot/src/scm.inp --force

open /home/at/git/PairPlot/data/iris.csv --quiet

list y = 1..4

# override default value
bundle opts = null
matrix opts.pointtype = {1,7,8}
matrix opts.pointsize = 2
matrix opts.size_scaling = 1.8
scalar opts.key = 1				# print values of factor as legend

string filename = "display" #"/home/at/test.png"		# optional: default 'display'
scalar err = PairPlot(y, variety, filename, &opts)
err


open credscore.gdt -q -p
list y = 1..4

scalar err = PairPlot(y, Selfempl, filename, &opts)
err
